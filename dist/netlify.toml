[build]
  publish = "dist"
  command = "npm install && npm run build"

[redirects]
  [[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200

[context.production.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"
